不要修改除了KN681生成的文件和代码

注释说明报表的实现目标，背景
注释说明报表需要实现的功能点分解
注释说明各个方法的功能
注释说明全局变量的含义
AI构建对象的时候如果查询的字段长度大于45，要考虑是否是类型正确

选择屏幕的文本元素处理


根据fs在报表头部记录本报表开发目标，功能说明

功能点分解，列出需要实现的功能

检查功能点是否都已经实现



报表头部用tables 关键字列出用到的表，并且注释

- 软删除字段判断
  - 查询语句要判断软删除标志（例如 LOEKZ = ' ' / DELETED = '0' / ACTIVE = 'X'）
  - 需要确认表结构是否存在逻辑删除字段，若有请加过滤条件
  - 删除操作若是逻辑删除，使用 UPDATE SET 标志位；物理删除使用 DELETE
  - 业务场景需区分软删除/物理删除，避免列表展示已作废记录